// import { ContributionsNFTData } from "../interfaces/contributionsnftinterface";

// export function transformContributionsNFTData(data: any[]): ContributionsNFTData[] {
//   const validData = data.filter((item) => Array.isArray(item) && item.length === 1);

//   const transformedData = validData.map(values => {
//     const transformedData = {
//       schemaID: values[0],
//     };
//     return transformedData;
//   });
//   return transformedData;
// }
